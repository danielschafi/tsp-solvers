repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.0
    hooks:
      # Linter — auto-fixes what it can, errors on the rest
      - id: ruff
        args: [--fix]
      # Formatter
      - id: ruff-format
